/**
 * 数组的定义使用关键字new
 * 方法一：给数组创建一个对象
 *      var name = new Array()
 *      name[size] = value
 * 
 * 方法二：
 *      var name = new Array(size)
 *      name[size] = value
 * 
 * 方法三：用字面量的方式声明数组
 *      var name = [value]
 */

 var a = new Array();
 a[11] = "hello world";
 console.log(a)

 var b = new Array(3);
 b[0] = 20;
 b[1] = false;
 b[2] = "hello";
 console.log(b)

 // 和python的列表类似
 // 最常用的声明方式
 
 var lst = ["小明",18,"65kg"];
 console.log(information);
